
The user wants the actual reply formatting rules — how Scholaris structures bullets, paragraphs, bold, spacing — as plain text. This lives in the chat edge function system prompt, not in memory. Let me grab it.

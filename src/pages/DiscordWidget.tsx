import { DiscordConnectWidget } from "@/components/DiscordConnectWidget";

const DiscordWidget = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-transparent p-4">
      <DiscordConnectWidget />
    </div>
  );
};

export default DiscordWidget;
